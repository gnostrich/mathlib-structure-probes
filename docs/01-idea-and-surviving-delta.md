# 01 — The idea and the surviving delta

## The idea (turns 1–3 of "gauge-covariant … / this thread")
An autonomous conjecturer-prover over Mathlib, analogous to the ETS tape being
the (N+1)-th clamped boundary node: the certified generator (certificate-as-
state, gated self-expansion, honest halt, meter-equals-certifier) "runs" over
Mathlib and expands it. Proposer = Aristotle. The novel piece is a **curation/
admission** layer: which proven theorems earn *structural authority* (become
anchors / extend the frontier) vs which are valid-but-inert.

## What ports vs what breaks
Ports: certificate-as-state ↔ clamped node; `expand_pd` ↔ progressive clamping;
meter=certifier ↔ no-decoder rule.
Breaks: (a) certification of a fixed kernel's PD ≠ proof search over propositions;
(b) self-ingestion — ETS gives the tape ZERO structural authority (I-8); kernel-
checking rescues *validity* but not *significance*; (c) coverage = completeness =
Gödel-blocked. AlphaFold misfit: no dense reward, no canonical target.

## The two-knob reframe (why the earlier "flat" reading was wrong knob)
The drift meter splits (Masani–Schoenberg) into:
- **slide** — symmetric / potential-descent surface quantity (proof length, depth-
  from-axioms, in-degree/centrality). Self-certifying, gameable → DIAGNOSTIC ONLY.
- **loop residue** — antisymmetric / holonomy: does a theorem *close a cycle*
  connecting previously-unjoined regions. Incorruptible → holds the VETO.
Earlier "meter over Mathlib is flat" (Hodge gradient R²≈0.015) was the SLIDE knob.
The LOOP knob is the b₁/V≈25.6, high, non-reciprocal side. Flip knobs, sign flips.

## Surviving delta after the closest-work sweep (see doc 02)
The *idea* is occupied (Kedrick et al. 2509.21899 = "cross-linking/hole-opening =
significance", validated at 34M-paper scale; Mathlib graph analysis occupied by
Li–Peng–Severini–Shafto 2604.24797; interestingness-meter-guided conjecturers
occupied by Fermat/AI-Mathematician/LeanConjecturer). Downgrade the core claim to
[occupied].

What survives = the **incorruptibility apparatus**, three things nobody in that
stack does together:
1. meter is NOT learned (Fermat *learns* interestingness = the fakeable case);
   a non-learned, gauge-invariant, kernel-checked holonomy residue.
2. prospective VETO inside the loop (Kedrick explains impact retrospectively via
   citations; never gates a generator), slide diagnostic-only, loop holds veto.
3. a measured FLOOR (warm/cold reproof; only cold-surviving residue gets authority).
Tag: [candidate-original — as discipline, narrow, contingent]. It lives or dies on
the double-dissociation surviving on the *dependency* graph (doc 03).
